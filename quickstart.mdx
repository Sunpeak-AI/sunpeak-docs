---
title: "Quickstart"
description: "Create your first ChatGPT App UI with sunpeak."
---

## New projects

<Steps>
  <Step title="Create a project">
    **Requirements:** Node.js 20+

    <Tabs>
      <Tab title="pnpm">
        ```bash
        pnpm dlx sunpeak new
        ```
      </Tab>
      <Tab title="npm">
        ```bash
        npx sunpeak new
        ```
      </Tab>
      <Tab title="yarn">
        ```bash
        yarn dlx sunpeak new
        ```
      </Tab>
    </Tabs>

    You will be prompted to name your project. We'll choose `my-app` for now.
  </Step>

  <Step title="Install dependencies">
    Navigate to your project directory and install dependencies:

    <Tabs>
      <Tab title="pnpm">
        ```bash
        cd my-app
        pnpm install
        ```
      </Tab>
      <Tab title="npm">
        ```bash
        cd my-app
        npm install
        ```
      </Tab>
      <Tab title="yarn">
        ```bash
        cd my-app
        yarn install
        ```
      </Tab>
    </Tabs>
  </Step>
  <Step title="Develop locally">
    Run the development server to use your local simulator:

    <Tabs>
      <Tab title="pnpm">
        ```bash
        pnpm dev
        ```
      </Tab>
      <Tab title="npm">
        ```bash
        npm run dev
        ```
      </Tab>
      <Tab title="yarn">
        ```bash
        yarn dev
        ```
      </Tab>
    </Tabs>

    Open `http://localhost:6767` to see the ChatGPT simulator running your app.

    <Tip>The simulator updates automatically as you edit your components!</Tip>
  </Step>

  <Step title="Develop in ChatGPT">
    Run the MCP server and a tunnel to allow ChatGPT to connect to it:

    <Tabs>
      <Tab title="pnpm">
        ```bash
        pnpm mcp

        # In another terminal, run a tunnel. For example:
        ngrok http 6766
        ```
      </Tab>
      <Tab title="npm">
        ```bash
        npm run mcp

        # In another terminal, run a tunnel. For example:
        ngrok http 6766
        ```
      </Tab>
      <Tab title="yarn">
        ```bash
        yarn mcp

        # In another terminal, run a tunnel. For example:
        ngrok http 6766
        ```
      </Tab>
    </Tabs>

    You can then connect to the tunnel `Forwarding URL` at the `/mcp` path (e.g. `https://your-random-subdomain.ngrok-free.dev/mcp`) from ChatGPT
    **in developer mode**: `User > Settings > Apps & Connectors > Create`.
  </Step>
</Steps>

<Card horizontal title="Project Structure" icon="page" href="/template/project-structure">
  Explore your new MCP App project.
</Card>
