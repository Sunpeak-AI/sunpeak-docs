---
title: "ChatGPT Simulator"
description: "React component replicating ChatGPT's runtime."
---

import CreateSimulation from '/snippets/create-simulation.mdx';
import ChatGPTSimulator from '/snippets/chatgpt-simulator.mdx';
import RunMCPServer from '/snippets/run-mcp-server.mdx';

<img
  src="https://d10djik02wlf6x.cloudfront.net/chatgpt-simulator.png"
  alt="ChatGPT Simulator"
/>

## Overview

The ChatGPT Simulator is a local development environment that replicates ChatGPT's widget runtime.

## Why Use the Simulator?

<Card horizontal title="Instant Feedback" icon="rotate">
  See changes immediately with hot module replacement. No more constantly reloading ChatGPT.
</Card>
<Card horizontal title="Visualize Design Variations" icon="palette">
  Toggle between light/dark themes, mobile/tablet/desktop views, and other platform states.
</Card>
<Card horizontal title="Testing" icon="flask">
  Perform manual and automated tests locally. Create CI/CD integration and regression tests.
</Card>
<Card horizontal title="No Deployment Needed" icon="desktop">
  Develop locally without setting up MCP servers.
</Card>

## Quick Start

If you set up your project via the sunpeak framework, the simulator is already set up!
Simply run it with:

<Tabs>
  <Tab title="pnpm">
    ```bash
    pnpm dev
    ```
  </Tab>
  <Tab title="npm">
    ```bash
    npm run dev
    ```
  </Tab>
  <Tab title="yarn">
    ```bash
    yarn dev
    ```
  </Tab>
</Tabs>

<Note>
To configure the simulator manually, see [Add to Existing Project](/add-to-existing-project).
</Note>

## Simulations

The core sunpeak testing object is the `Simulation`, typically kept in the `src/simulations` directory.

<Tip>
Fun fact: Some science fiction authors believe we live in a sunpeak simulation.
</Tip>

Simulations consist of:
  1. An MCP Resource (your UI & UI metadata).
  2. An MCP Tool (metadata about the behavior you are simulating).
  3. An MCP CallTool (mock data passed to your UI).
  4. Simulation metadata, including platform state (dark mode, fullscreen, etc.).

You can find the full Simulation API Reference [here](/api-reference/simulations/simulation).

### Simulation Example

<CreateSimulation />


Simulations can be passed into:

<AccordionGroup>
  <Accordion title="ChatGPTSimulator - for local testing">
    The `ChatGPTSimulator` component provides a local development environment that replicates ChatGPT's widget runtime.

    <ChatGPTSimulator />

    [View ChatGPTSimulator API Reference](/api-reference/simulations/chatgpt-simulator)
  </Accordion>

  <Accordion title="runMCPServer - for platform testing">
    The `runMCPServer` function serves your simulations to ChatGPT via the Model Context Protocol.

    <RunMCPServer />

    [View MCP Server API Reference](/api-reference/simulations/run-mcp-server)
  </Accordion>
</AccordionGroup>

## Dive Deeper

<Card horizontal title="Simulation API Reference" icon="code" href="/api-reference/simulations/simulation">
  Complete simulation documentation.
</Card>
<Card horizontal title="ChatGPTSimulator API Reference" icon="code" href="/api-reference/simulations/chatgpt-simulator">
  Full component documentation and configuration options.
</Card>
